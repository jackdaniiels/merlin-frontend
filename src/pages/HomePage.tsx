import { Link } from "react-router"

export const HomePage = () => {
    // const navigate = Navigate
    return (
        <>
            <div>HomePage</div>
            <Link to="/about">About</Link>
        </>
    )
}
